# ── Large data artifacts — distributed via Zenodo, NEVER committed ───────────
data/unified_database*.csv
data/*.csv.gz
**/unified_database*.csv
# Raw Timeloop sweep outputs (90+ GB) — regenerated by the provenance demo (T3)
**/outputs/
**/flashattn_outputs/
*.tar
*.tar.gz

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.db_cache/
.ipynb_checkpoints/
.pytest_cache/

# ── Run logs / scratch ───────────────────────────────────────────────────────
ae_logs/
*.log

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store

# AE notebook / build outputs (regenerated)
notebooks/_figures/
notebooks/.ipynb_checkpoints/
bundle/
src/scripts/arch_impl/images/
src/scripts/**/_repro_*.csv
src/unified_database.csv

# reviewer-run outputs (not shipped)
src/train_test/results/smoke_*
src/train_test/results/2*
src/scripts/arch_impl/*repro*
notebooks/_figures/

# --- public-repo additions ---
src/scripts/results/
src/unified_database.csv
*.bak
*.bak_*
bundle*/
*.csv.gz
*.feather
**/flashattn_database*.csv
**/pim_database*.csv
# vit_pim_database.csv (36K) is a required input for generate_paper_fig14.py,
# which emits case_study/av_vision_results.csv behind the paper's AV macros.
# Only the large side databases are excluded.
convergence_runs/
